[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build", "echo '=== dist gerado ===' && find dist -type f | head -30"]

[start]
cmd = "node dist/server/index.js"
