[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build:client", "ls -la dist/ || echo 'dist nao existe'", "ls -la dist/client/ || echo 'dist/client nao existe'"]

[start]
cmd = "node_modules/.bin/tsx server/index.ts"
