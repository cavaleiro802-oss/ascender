[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = [
  "node diagnose.mjs",
  "npm run build:client",
  "node diagnose.mjs"
]

[start]
cmd = "npx tsx server/index.ts"

